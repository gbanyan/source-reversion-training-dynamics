# Data and code availability statement

## Submission-ready template

> Analysis code, frozen configuration files, canonical machine-readable result
> summaries, selected raw JSON artifacts, and the rendered figures for this
> study are available at:
> `https://github.com/gbanyan/source-reversion-training-dynamics`
> (tag `v0.1.1-review`).
> The public repository does not redistribute the upstream language-model
> weights or third-party corpora. The complete semantic raw matrix is
> available at the repository link when redistribution is permitted; otherwise
> the repository contains the validated pooled summaries and derived tables,
> and access to the raw matrix is subject to the applicable data terms.

If the raw semantic matrix is included in a later release, update the statement to name
the exact path and file format. If it cannot be shared, retain the reason and
describe the derived artifacts that remain public.
